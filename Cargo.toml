[package]
edition = "2024"
name = "kladdvara"
version = "0.1.0"
license = "GPL-3.0-or-later"

[dependencies]
crossterm = "0.29.0"

[profile.release]
codegen-units = 1
lto = true
opt-level = 's'
panic = "abort"
strip = true
